services:
  proxy:
    build: .
    user: "${PROXY_UID:-1000}:${PROXY_GID:-1000}"
    ports:
      - "${PROXY_PORT:-8080}:8080"
      - "${VIEWER_PORT:-9999}:9999"
    volumes:
      - ./certs:/app/certs
    restart: unless-stopped
